# CD Command Center — Project Pipeline

Living document tracking outstanding workstreams. Organized by 
category, not priority. Priority is decided per session based on 
current needs, not enshrined in the doc.

**Maintenance rules:**
- When an item ships, remove it from this doc in the same commit 
  that ships it. The commit message carries the history.
- When a new workstream is identified, add it to the appropriate 
  category with the same level of detail as existing items.
- When a scope question is unresolved, name it explicitly in the 
  item (see D1 for the pattern).
- Do not add items for bugs that should just be fixed — this is for 
  workstreams, not a bug tracker.
- Do not let categorization become a project of its own. If an item 
  doesn't fit cleanly, put it in the category it's closest to and 
  move on.

---

cd-command-center project pipeline

**Postgres readiness:**
- **Execution sequence: see `docs/postgres-migration-plan.md`** — 6-phase plan, 22-37 hours across 7-9 dedicated sessions. Phase order: silent handlers → operational logging → raw-? sweep → lastrowid+dict(row) → SQLite-only functions → schema/data reconciliation. Working agreement requires 3-6 hour dedicated sessions, not weeknight slots.
- Audit evidence: `docs/debug/postgres_readiness_inventory_2026-04-22.md` (9 known bug classes, ~70 confirmed sites). When the plan and the inventory disagree, re-run the inventory.
- **B1, B2, B4, B5, B6 below are now consolidated under the phased plan.** They remain in this doc as detail (sites, line numbers, decisions) but the execution sequence lives in the migration plan, not here. Do not start any of them as standalone sessions without first consulting the plan to confirm prerequisites are met.

Category A — Shipped pipelines that are broken or incompletePipelines with known gaps or bugs blocking real use today. Highest urgency by default.
A1. FastAPI endpoints currently broken on Postgres. User-notes CRUD (GET/POST/PUT/DELETE) and get_census_tract raise psycopg2 syntax errors on raw ? placeholders — return HTTP 500 on any request. get_school_by_id and get_nmtc_project_by_id silently return 404 (silent-exception handler swallows the same bug). get_bookmarks (GET /notes/bookmarks/all) silently returns [] due to dict(row) on psycopg2's default tuple cursor (different bug class — see B6). Bookmarks write/check/delete paths fixed in d7855ee (POST, DELETE, is_bookmarked). Live user-facing impact if the dashboard is being served against Postgres. Priority depends on whether the dashboard is actually deployed against Postgres today.
A2. ECE pipeline loaded with random state. 4,556 rows, Colorado only, legacy from earlier experimentation. Postgres-compat fix shipped in commit 8a8d848; the pipeline works, just with irrelevant data. Add GA alongside CO, and any other states for which there is similar data.
A3. Cross-backend drift between Postgres and SQLite. Observed across multiple tables: irs_990 (4,126 vs 4,123), schools charter linkage (5,190 vs 5,187), Atlanta schools search (157 vs 150), Fulton search (191 vs 160), cdes (11 vs 7), fqhc (43 vs 42). Pattern, not one-off. Possible causes: loader runs hitting only one backend, failed upserts swallowed by silent handlers, pre-Postgres-compat data that never re-ingested. Needs a dedicated reconciliation session — audit every table, pick canonical source per table, resync.

Category B — Systematic bug-class cleanups
Mechanical work following proven templates. Sessions close a bug class rather than ship a feature.
B1. Remaining upsert_* Postgres-compat fixes. Five upsert_* functions in db.py still carry the adapt_sql miss pattern (plus potentially COALESCE qualification and dict(row) issues per the three-bug sweep template): upsert_nmtc_project, upsert_cde_allocation, upsert_cdfi, upsert_enrollment_history, upsert_lea_accountability. Same template as census tracts (c1fc5b3), schools (c87742b), FQHC (0b63cd7), ECE (8a8d848), 990s (c1c562c), financial_ratios (c0dd59a), cdfi_award (fa915ef). Mechanical. Assume ~1.5-2x the initial inventory count based on this session's pattern — diagnosis typically under-counts. **See `docs/debug/postgres_readiness_inventory_2026-04-22.md` for the full Postgres readiness picture before starting.**

upsert_state_program — NOT the same template. Uses SQLite-only INSERT OR IGNORE syntax which adapt_sql does not rewrite. Requires either (a) rewriting the function to use INSERT ... ON CONFLICT DO NOTHING (needs schema-level decision on conflict key), or (b) extending adapt_sql to rewrite INSERT OR IGNORE → ON CONFLICT DO NOTHING on Postgres (shared helper change, cross-function blast radius, requires a sweep of other INSERT OR IGNORE sites first). Tracked separately as B5.
B2. Broader adapt_sql sweep (Phase 8). ~28 raw-? sites across the repo beyond the 990s commit's coverage. Breakdown: 7 FastAPI loud-crash sites (tracked in A1), 2 FastAPI silent-404 sites (tracked in A1), 6 active ETL blockers (fetch_bmf_eins.py:357/395/401, load_census_tracts.py:338-339/357, load_ejscreen.py:392-393, load_opportunity_zones.py:185-186, patch_pct_asian.py:117-120/152-153), 8 dead-code/archived sites (tearsheet, documents). Inventory starter at docs/debug/chained_execute_inventory_2026-04-21.md. Mandate fresh full-repo re-grep at session start — multi-line regex misses sites and the inventory is approximate within ±3. **See `docs/debug/postgres_readiness_inventory_2026-04-22.md` for the full Postgres readiness picture before starting.**
B3. Chained cursor.execute(...).fetchone()/.fetchall() cleanup (Phase 7 completion). 8 sites remaining across 4 files. 1 ACTIVE: compute_financial_ratios.py. 7 LATENT: fetch_lea_accountability.py (2, upstream API 500'ing), fetch_nmtc_award_books.py (3, annual PDF job), patch_pct_asian.py (2, one is CHAIN+PLACEHOLDER requiring adapt_sql ride-along). LATENT verification needs py_compile + import + structural checks; defer end-to-end verification until upstream/input conditions allow. Small workstream; 2-3 commits, ~1 hour total.
B4. Silent exception handler audit. except Exception: continue / except Exception: return {} / except Exception: pass patterns throughout db.py and helpers with no logging of the underlying error. This pattern is the mechanism that's been hiding adapt_sql bugs, dict(row) bugs, chained-execute bugs from every verification attempt across the session. Promoted from "future hygiene" to "do concurrently with or before B2." Architectural, not mechanical — each handler site needs individual judgment: log-and-continue, log-and-raise, or remove the handler entirely. **Inventory shipped 2026-04-22 at `docs/debug/postgres_readiness_inventory_2026-04-22.md` — 141 total `except Exception` sites; ~20 DANGEROUS in db.py masking known bug classes (raw-?, dict(row), upsert failures); ~40 RISKY readers; ~80 LOW-RISK in ETL with logging.** Recommended action: add logging to ~20 DANGEROUS sites first; estimated ~6-10 hours.
B5. INSERT OR IGNORE → ON CONFLICT DO NOTHING sweep. SQLite-only syntax. Inventory shipped 2026-04-22 at docs/debug/insert_or_ignore_inventory_2026-04-22.md. **See also `docs/debug/postgres_readiness_inventory_2026-04-22.md` for the full Postgres readiness picture.** 3 total sites. Status: (1) save_bookmark FIXED in d7855ee (INSERT OR IGNORE → ON CONFLICT(entity_type, entity_id) DO NOTHING + adapt_sql wrap; delete_bookmark and is_bookmarked adapt_sql wraps included in same commit); (2) etl/load_cra_lending.py:330 — no action needed (Postgres path already correctly uses ON CONFLICT DO NOTHING via execute_values); (3) upsert_state_program REMAINING — requires schema work (state_programs has no UNIQUE constraint; proposed natural key (state, program_name)). 0 INSERT OR REPLACE sites. Recommended approach: per-function rewrite (Approach A) — confirmed by the save_bookmark fix. Helper-change approach (extend adapt_sql to rewrite INSERT OR IGNORE at statement level) rejected — does not resolve the no-UNIQUE-constraint site, touches every adapt_sql caller for one-line savings. Remaining effort: ~1.5-2 hours for state_programs schema migration (choose natural key, dedupe existing SQLite rows that accumulated because the old INSERT OR IGNORE was a silent accept-all against AUTOINCREMENT PK, add UNIQUE constraint, rewrite upsert_state_program, reload seed). Side finding surfaced but flagged out-of-scope in inventory: cur.lastrowid on Postgres (see B6).

B6. cur.lastrowid and dict(row) on Postgres. save_user_note (db.py:3071) and save_document (db.py:3170) execute bare INSERTs and then return cur.lastrowid to FastAPI callers. api/routers/notes.py:48 returns that id to the dashboard as {"note_id": ...}. psycopg2's cursor.lastrowid returns None for INSERT statements (Postgres tables have no OID by default), so both endpoints silently return {"note_id": null} on Postgres. Related sibling bug: get_bookmarks (db.py:3108) and get_user_notes list-building use [dict(row) for row in cur.fetchall()] — psycopg2's default cursor returns plain tuples, not sqlite3.Row objects, so dict(row) raises TypeError which the surrounding silent except swallows, returning []. Same bug class: SQLite-specific row/cursor conventions that fail differently on psycopg2's default cursor. Fix approach: either switch get_connection() to psycopg2.extras.RealDictCursor (large blast radius — affects every SELECT path in db.py; changes row access from tuple-index to dict-key everywhere) or add a small row_to_dict(cur, row) helper + use RETURNING id for INSERT paths that need lastrowid. Decision needed before implementation. Flagged by the B5 inventory (2026-04-22); standalone workstream. Surfaced again by the save_bookmark verification (get_bookmarks on Postgres returned [] post-fix, proving this is a separate unblocked issue). **Per `docs/debug/postgres_readiness_inventory_2026-04-22.md`: cur.lastrowid is 3 sites total — adds L1 `db.py:1292 log_load_start` (NEW finding: pipeline run logging is silently broken on Postgres because run_id = None means log_load_finish UPDATEs zero rows; every pipeline shows status 'running' forever). dict(row) sweep is incomplete — at least 6 confirmed (`get_school_by_id`, `get_fqhc_by_id`, `get_ece_by_id`, `get_nmtc_project_by_id`, `get_user_notes`, `get_bookmarks`) plus 8 more in the latent tearsheet path; needs a fresh full-repo grep.**

Category C — New data ingestion (expand existing pipelines)
Adding real operator-intelligence data to pipelines that currently have shallow or directory-only data. Each is a full multi-hour session minimum; requires source research and schema design.
C1. FQHC UDS ingestion. **DONE 2026-05-04 (initial load); UIID gaps closed 2026-05-06.** Alembic 9e3b590fa748 added `fqhc.org_bhcmis_id` + `fqhc.health_center_grant_number` (backfilled 18,727/18,830 sites) and `fqhc_uds_reports` (50 curated columns + `raw_metrics_json` JSONB). Loader `etl/load_fqhc_uds.py` uses HRSA's UIID-coded columns with case-insensitive lookup (handles 2020-2023 `T9D_*` / `T9E_*` upper vs 2024 `T9d_*` / `T9e_*` lower) and a `_LEGACY_VERBOSE_TO_UIID` remap for 2020-vintage Table 9E (which used verbose column names instead of UIIDs). FastAPI: `/fqhc/{id}/uds`, `/fqhc/{id}/uds/history`, `/fqhc/uds/summary`. **Loaded:** 6,993 grantee-year reports across 1,534 grantees, 2020-2024. Post-fix coverage of revenue: bphc_grant_revenue ~1,370/yr (2020-2023), other_federal ~700-1,250/yr, state_local ~1,150/yr, private ~1,100/yr — was 0 across the board for those years before 2026-05-06 fix. Remaining NULLs in `total_costs` and `medical_visits` reflect real reporting gaps (only ~115/1,375 grantees filed Table 8A in 2020; jumped to 1,512 by 2024).
C2. ECE operator intelligence. Current ECE table is licensing-directory-only — center identity, address, licensed capacity, licensing status. Missing: actual enrollment (vs licensed capacity), subsidy participation (CCDBG, state voucher programs, Head Start partnership, Pre-K funding source), quality ratings (state QRIS star ratings, NAEYC accreditation, CLASS scores), staffing ratios, child demographics, financials. Unlike FQHC where UDS provides a national source, no single national ECE operator-intelligence source exists — QRIS, subsidies, Head Start, and financials all come from different state and federal systems. Multi-source, state-by-state project. Substantially larger scope than C1.
C3. NMTC end-use classification. Current `nmtc_projects` (CDFI Fund Public Data) and `nmtc_coalition_projects` (Coalition TLR) carry only generic project_type values (NRE / RE / SPE / CDE) and `purpose of investment` financing-type strings — no end-use category distinguishing charter schools, FQHCs, ECE centers, supportive housing, etc. CDFI Fund publishes an annual Compliance Report (separate Excel from the Public Data file) with detailed end-use categories per CDFI project ID. Adding it means: new `etl/load_nmtc_compliance.py` loader, new sibling table keyed on CDFI project ID, view joining through to `nmtc_projects`. Without this, downstream questions like "which TN charter schools received NMTC investment?" cannot be answered — only the broader proxy "which tracts had both an NMTC project and a charter school." Half-day for the loader; annual refresh cadence. Surfaced 2026-05-03 by the TEFF/TCSC analysis (`analyses/teff_tcsc_2026-04/`).
C4. Charter school `year_opened` backfill. `schools.year_opened` is null for every charter school because the Urban Institute directory endpoint (`etl/fetch_nces_charter_schools.py:357`) hardcodes it to None — the field is not in that endpoint. Two source options: (a) NCES ELSI bulk export (national, free; requires a new ELSI-export ingest path with no current Urban API mirror); (b) per-state DOE charter directories (varying formats). Scope decision needed: national-via-ELSI gives full coverage at the cost of a new loader surface; per-state stops at TN/GA-first and accepts patchy coverage. School age is a survival proxy in charter underwriting, so this is value-bearing data, not a nice-to-have. Surfaced 2026-05-03.
C5. `seasch` repo-wide coverage gap. State-assigned school IDs are populated for only ~10.8% of `schools` rows (10,576 / 97,750). The gap is upstream — Urban Institute's API returns `seasch` for a subset of states/years; nothing is broken on our side (no failed join). TN happens to be 100% covered. Closing the gap requires per-state DOE pulls, same shape as C4 above. Scope question: do we need `seasch` nationally, or only in states where charter activity is being underwritten (TN, GA, plus any active deal state on demand)? Latter is the cheaper default. Surfaced 2026-05-03.

Category D — New pipelines (entirely new data domains)
Building things that don't exist today. Biggest, most open-ended workstreams.
D1. Form 990-PF ingestion for PRI. Current irs_990 table holds Form 990 filings from public charities only (charter LEAs, FQHCs, CDEs). Private foundations file Form 990-PF, which includes Part IX-B (Program-Related Investments). PRI work requires a separate pipeline: pull IRS 990-PF bulk XML from AWS S3, parse Part IX-B, store raw description text (no LLM parsing, per prior hallucination issues). Different filer universe, different XML schema, different table.
Scope decision needed: Original scope was GA-headquartered foundations only (~500-1,000 filers) as a prerequisite for the GSIC Georgia market mapping deliverable. You've expressed interest in expanding to US scope (~100,000 filers) — that's a fundamentally different-sized pipeline (100-200x more data, different design constraints around storage and incremental ingestion). Before starting, decide explicitly: is GSIC still the primary customer with its timeline, or is this a longer open-ended ambition? They're different projects. Don't start until that's clear.
D2. 990 asset-class expansion. Current 990 coverage maps to existing asset-class pipelines (charter schools, FQHCs, CDEs). As the command center extends to new asset classes — supportive housing, workforce development, senior services, community health worker orgs, arts/cultural institutions, charter management organizations — the 990 ingestion needs to broaden. Each new asset class requires: (1) an EIN source list (IRS Exempt Organizations Business Master File, NTEE code filter, sector-specific directories), (2) a corresponding facility/entity table, (3) a join from irs_990.ein to that table's EIN column. Not a single project — an ongoing expansion done per asset class as underwriting demand drives it.
D3. Historic Tax Credit (HTC) integration. No current pipeline supports HTC underwriting. Adding it means: NPS/SHPO certification data (Part 1, 2, 3 applications), qualified census tract overlay for HTC-boosted projects, rehabilitation expenditure tracking, HTC/NMTC twinning analysis (common for deeply-distressed historic redevelopment deals). Scope before first deal that needs it — retrofitting underwriting data into an in-flight deal is more painful than having it ready.
